#pragma once

#include <iostream>
#include <vector>

int binarySearch(std::vector<int> &v, int left, int right, int n);
void insertionSort(std::vector<int> &v, int n);
void subMerge(std::vector<int> &v, int p, int r);